<!-- Workspace Copilot instructions (auto-created) -->
- Verify the project scaffold and build using the provided tasks and CMake files.
- Run the tests with `ctest --output-on-failure` in `build/`.
- If additional scaffolding is requested, update this file accordingly.
