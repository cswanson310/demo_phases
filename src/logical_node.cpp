#include "logical_node.h"
#include <iostream>

// No registration system needed anymore - we use template specialization
// Each concrete logical node header provides its own createLogicalNode<ParamType> specialization
